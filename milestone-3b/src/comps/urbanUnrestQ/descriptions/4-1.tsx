const description = () => {
  return (
    <p>
      Assuming the maxdeath is true for each event available (or the mindeath is
      true for events with a death flag but an unknown maxdeath). Find the total
      amount of deaths that have occured from Urban Unrest events in Rwanda.
      Then, using a bar chart, compare this total to the total amount of deaths
      that have occured from Urban Unrest events in every country besides
      Rwanda.
    </p>
  );
};

export default description;
