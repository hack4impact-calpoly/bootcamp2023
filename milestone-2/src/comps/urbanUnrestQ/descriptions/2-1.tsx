const description = () => {
  return (
    <p>
      Assuming the maxdeath is true for each event available (or the mindeath is
      true for events with a death flag but an unknown maxdeath), visualize and
      compare the following distribution:
      <ol>
        <li>
          Using data from all events globally except for Armed Battle/Clash type
          events in Rwanda, what proportion of all deaths resulted from each
          problem type?
        </li>
      </ol>
    </p>
  );
};

export default description;
